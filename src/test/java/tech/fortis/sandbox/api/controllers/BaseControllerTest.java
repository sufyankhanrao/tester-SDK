/*
 * FortisAPILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package tech.fortis.sandbox.api.controllers;

import org.junit.AfterClass;
import org.junit.BeforeClass;
import org.junit.Rule;
import org.junit.rules.Timeout;
import tech.fortis.sandbox.api.Environment;
import tech.fortis.sandbox.api.FortisAPIClient;
import tech.fortis.sandbox.api.testing.HttpCallbackCatcher;

/**
 * Base class for all test cases.
 */
public class BaseControllerTest {
    /**
     * Test configuration.
     */
    public static final int REQUEST_TIMEOUT = 30;

    public static final double ASSERT_PRECISION = 0.01;
    
    /**
     * Global rules for tests.
     */
    @Rule
    public Timeout globalTimeout = Timeout.seconds(REQUEST_TIMEOUT);
    
    /**
     * Test fixtures,
     * Used to serve as HttpCallback and to capture request & response.
     */

    protected static HttpCallbackCatcher httpResponse;
    
    /**
     * Setup test.
     */
    @BeforeClass
    public static void setUp() throws Exception {
        httpResponse = new HttpCallbackCatcher(); 
    }

    /**
     * Tear down test.
     */
    @AfterClass
    public static void tearDown() throws Exception {
        httpResponse = null;
    }

    /**
     * Create test configuration from Environment variables.
     */
    protected static FortisAPIClient createConfigurationFromEnvironment() {
        FortisAPIClient.Builder builder = new FortisAPIClient.Builder();

        final String environment = System.getenv("FORTIS_API_LIB_ENVIRONMENT");
        final String timeout = System.getenv("FORTIS_API_LIB_TIMEOUT");
        final String customHeaderUserId = System.getenv("FORTIS_API_LIB_CUSTOM_HEADER_USER_ID");
        final String customHeaderUserApiKey =
                System.getenv("FORTIS_API_LIB_CUSTOM_HEADER_USER_API_KEY");
        final String customHeaderDeveloperId =
                System.getenv("FORTIS_API_LIB_CUSTOM_HEADER_DEVELOPER_ID");

        if (customHeaderUserId != null && customHeaderUserApiKey != null && customHeaderDeveloperId != null) {
            builder.customHeaderAuthenticationCredentials(customHeaderUserId, customHeaderUserApiKey, customHeaderDeveloperId);
        }
        if (environment != null) {
            builder.environment(Environment.fromString(environment));
        }
        if (timeout != null) {
            builder.httpClientConfig(configBuilder -> configBuilder.timeout(
                    Long.parseLong(timeout)));
        }
        return builder.build();
    }

    
    /**
     * Create test configuration.
     */
    protected static FortisAPIClient createConfiguration() {
        // Set Configuration parameters for test execution
        FortisAPIClient config = createConfigurationFromEnvironment();
        config = config.newBuilder()
                .httpCallback(httpResponse)
                .customHeaderAuthenticationCredentials("11ec32a6aea0e78ea5638a19",
                        "11ec8d9bdf472c4aba3adfc9", "260eJswa")
                .build();
        return config;
    }
}
